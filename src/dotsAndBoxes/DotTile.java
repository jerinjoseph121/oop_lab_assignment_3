package dotsAndBoxes;

import common.tiles.Tile;

/** Dot/space node on the Dots & Boxes grid. */
public class DotTile extends Tile {
    public DotTile(int row, int col) {
        super(row, col);
    }
}
